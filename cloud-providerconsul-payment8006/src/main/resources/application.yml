server:
  port: 8006
spring:
  application:
    name: consul-provider-payment
  cloud:
    # consul注册地址
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}